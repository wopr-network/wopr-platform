# Local development environment — safe defaults, no real secrets needed.
# Copy to .env: cp .env.dev .env

# --- Domain (unused locally, but required by code) ---
DOMAIN=localhost

# --- Stripe (test mode) ---
STRIPE_SECRET_KEY=sk_test_fake_local_dev
STRIPE_WEBHOOK_SECRET=whsec_fake_local_dev
STRIPE_DEFAULT_PRICE_ID=price_fake_local_dev

# --- Auth ---
BETTER_AUTH_SECRET=local-dev-secret-not-for-production
BETTER_AUTH_URL=http://localhost:3100

# --- Email (disabled locally — logs instead of sending) ---
RESEND_API_KEY=re_fake_local_dev
RESEND_FROM_EMAIL=noreply@localhost

# --- Bot image (for fleet tests requiring Docker) ---
WOPR_BOT_IMAGE=ghcr.io/wopr-network/wopr:latest

# --- Database paths (SQLite, auto-created) ---
BILLING_DB_PATH=./data/platform/billing.db
QUOTA_DB_PATH=./data/platform/quotas.db
AUDIT_DB_PATH=./data/platform/audit.db
CREDITS_DB_PATH=./data/platform/credits.db
SNAPSHOT_DB_PATH=./data/snapshots.db
TENANT_KEYS_DB_PATH=./data/platform/tenant-keys.db
METER_WAL_PATH=./data/meter-wal.jsonl
METER_DLQ_PATH=./data/meter-dlq.jsonl
AUTH_DB_PATH=./data/auth.db
BACKUP_DB_PATH=./data/platform/backup-status.db
DATABASE_PATH=./data/platform.db

# --- UI origin (for CORS) ---
UI_ORIGIN=http://localhost:3000
PLATFORM_DOMAIN=localhost

# --- Node ---
NODE_ENV=development
PORT=3100
