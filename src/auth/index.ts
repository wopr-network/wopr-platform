/**
 * Auth — Better Auth integration (future migration from core).
 *
 * Better Auth is currently integrated in the core daemon (WOP-261, Done).
 * This module is the target for migrating auth to the platform backend,
 * where it belongs alongside tenancy and billing.
 */
